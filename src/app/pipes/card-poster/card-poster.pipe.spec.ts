import { CardPosterPipe } from './card-poster.pipe';

describe('CardPosterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardPosterPipe();
    expect(pipe).toBeTruthy();
  });
});
